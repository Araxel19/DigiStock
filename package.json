{
  "name": "digistock",
  "version": "1.0.0",
  "description": "DigiStock - Sistema de digitalización de inventario",
  "private": true,
  "workspaces": [
    "backend",
    "business-logic"
  ],
  "scripts": {
    "build": "npm run build --workspace=business-logic && npm run build --workspace=backend",
    "build:backend": "npm run build --workspace=backend",
    "build:business-logic": "npm run build --workspace=business-logic",
    "install:all": "npm install --workspaces"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "sonar-scanner": "^3.1.0",
    "typescript": "^5.1.3"
  },
  "dependencies": {
    "@nestjs/platform-socket.io": "^10.4.20",
    "@nestjs/websockets": "^10.4.20",
    "@types/socket.io-client": "^1.4.36",
    "chart.js": "^4.5.0",
    "jspdf": "^3.0.3",
    "jspdf-autotable": "^5.0.2",
    "socket.io-client": "^4.8.1",
    "vue-chartjs": "^5.3.2",
    "xlsx": "^0.18.5"
  }
}
